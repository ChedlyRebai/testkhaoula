{% extends 'admin/base_admin.html.twig' %}

{% block title %}Détails du cours{% endblock %}

{% block admin_content %}
<div class="container-fluid">
    <h1 class="h3 mb-4 text-gray-800">📖 Détails du cours</h1>

    <div class="card shadow mb-4">
        <div class="card-body">
            <table class="table table-bordered">
                <tbody>
                    <tr><th>Id</th><td>{{ cours.idCours }}</td></tr>
                    <tr><th>Titre</th><td>{{ cours.titre }}</td></tr>
                    <tr><th>Description</th><td>{{ cours.description }}</td></tr>
                    <tr><th>Date création</th><td>{{ cours.dateCreation ? cours.dateCreation|date('d/m/Y H:i') : '-' }}</td></tr>
                    <tr><th>Date publication</th><td>{{ cours.datePublication ? cours.datePublication|date('d/m/Y H:i') : '-' }}</td></tr>
                    <tr><th>Visibilité</th>
                        <td>
                            {% if cours.visibilite %}
                                <span class="badge bg-success">Visible</span>
                            {% else %}
                                <span class="badge bg-secondary">Caché</span>
                            {% endif %}
                        </td>
                    </tr>
                    <tr><th>Contenu</th><td>{{ cours.contenu }}</td></tr>
                    <tr><th>Type contenu</th><td>{{ cours.typeContenu }}</td></tr>
                    <tr><th>URL contenu</th>
                        <td>
                            {% if cours.urlContenu %}
                                <a href="{{ cours.urlContenu }}" target="_blank" class="text-decoration-none">🔗 Lien</a>
                            {% else %}
                                -
                            {% endif %}
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

    <div class="d-flex justify-content-between">
        <a href="{{ path('admin_cours_edit', {'id_cours': cours.idCours}) }}" class="btn btn-warning">
            ✏️ Modifier
        </a>
        <a href="{{ path('admin_cours_index') }}" class="btn btn-secondary">
            ⬅️ Retour à la liste
        </a>
    </div>
</div>
{% endblock %}
