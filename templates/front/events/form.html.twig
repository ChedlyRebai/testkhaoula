{% extends 'front/base_edubin.html.twig' %}

{% block title %}Ajouter un événement - EduSync{% endblock %}

{% block body %}
    <div class="page-header bg-light py-5">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-8">
                    <h1 class="h2 mb-0">Ajouter un nouvel événement</h1>
                </div>
                <div class="col-lg-4 text-lg-right mt-3 mt-lg-0">
                    <a href="{{ path('event_list') }}" class="btn btn-outline-secondary">← Annuler</a>
                </div>
            </div>
        </div>
    </div>

    <div class="container py-5">
        <div class="text-center my-5">
            <h3>Formulaire en cours de développement</h3>
            <p class="text-muted">Le formulaire d'ajout d'événement sera bientôt disponible</p>
        </div>
    </div>
{% endblock %}
